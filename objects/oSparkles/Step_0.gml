//Scale to, and follow the card given
x = card.x
y = card.y
image_xscale = card.image_xscale
image_yscale = card.image_yscale
image_angle = card.image_angle

if (card.dropped == true)
{
	depth = card.depth
}